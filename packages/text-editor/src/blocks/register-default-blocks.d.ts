import { BlockRegistry } from './block-registry';
/** Registers built-in block definitions (paragraph, heading, list_item, table, embed). */
export declare function registerDefaultBlocks(registry: BlockRegistry): void;
/** New registry with {@link registerDefaultBlocks} already applied. */
export declare function createDefaultBlockRegistry(): BlockRegistry;
//# sourceMappingURL=register-default-blocks.d.ts.map